# Search Components

Vector search UI components.

- `search-bar.tsx` - Input with search button and index status
- `search-results.tsx` - Result cards with proposal details
- `similarity-badge.tsx` - Color-coded similarity score chip
