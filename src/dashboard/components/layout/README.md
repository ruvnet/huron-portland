# Layout Components

App shell components providing the dashboard frame.

- `shell.tsx` - Combines sidebar and header into app shell
- `sidebar.tsx` - Navigation sidebar with route highlighting
- `header.tsx` - Top bar with theme toggle
