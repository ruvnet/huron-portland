# Proposal Components

Components for displaying and managing grant proposals.

- `proposal-card.tsx` - Card view with title, PI, budget, tags
- `proposal-table.tsx` - Table view using HeroUI Table
- `status-badge.tsx` - Color-coded status chip for 21 proposal states
