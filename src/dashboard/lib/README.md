# Lib

Shared utilities, types, and client libraries.

- `types.ts` - TypeScript type definitions (Proposal, SearchResult, etc.)
- `constants.ts` - App constants, status colors, navigation items
- `api/` - API client with mock data fallback
- `wasm/` - rvlite WASM loader and vector index
