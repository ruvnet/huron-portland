# WASM

rvlite WASM vector search integration.

- `rvlite-loader.ts` - Loads WASM module with JS cosine similarity fallback
- `vector-index.ts` - Builds search index from proposals, performs similarity search
