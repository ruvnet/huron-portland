# API

Data fetching layer with mock/real switching.

- `client.ts` - API client that switches between mock and real backend based on env var
- `mock-data.ts` - 12 mock proposals with derived aggregate functions
